import { OpaqueToken } from '@angular/core';

export const applicationParams = new OpaqueToken('applicationParams');
export const applicationOriginalUrl = new OpaqueToken('applicationUrl');
export const applicationCache = new OpaqueToken('applicationCache');
